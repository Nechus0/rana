{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Was das Hauptfenster darf. Bewusst knapp gehalten: Dateiauswahl für Sicherung und Export, Öffnen von Verweisen im Browser, und die eigenen Befehle. Kein freier Dateizugriff, kein Fernzugriff.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:allow-save",
    "dialog:allow-open",
    "dialog:allow-message",
    "opener:allow-open-url"
  ]
}
